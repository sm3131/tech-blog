<div class="row justify-content-between">
    <div class="mt-4 col-4">
        <a href="/post/{{id}}" target="_blank">{{title}}</a>
    </div>
    <div class="mt-4 col-5">
        <p class="date">By {{user.username}} on {{created_at}}</p>
    </div>
</div>
<div class="col-12">
    {{post_text}}
    <a href="/post/{{id}}" class="d-block mt-2">{{comments.length}} Comment(s)</a>
</div>